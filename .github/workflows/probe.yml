name: public-probe
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
jobs:
  p:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: |
          echo "public-repo probe fired"
          echo "UTC: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          echo "cron: ${{ github.event.schedule }}"
